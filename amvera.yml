meta:
  environment: python
  toolchain: pip
  version: 3.11
build:
  requirementsPath: requirements.txt
run:
  command: gunicorn irina_realty.wsgi:application
  persistenceMount: /data
  containerPort: 8000
